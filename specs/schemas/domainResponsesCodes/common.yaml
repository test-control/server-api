type: object
required:
  - errorCodes
additionalProperties: false
properties:
  errorCodes:
    type: object
    properties:
      resourceNotFound:
        type: string
        enum: ['resource-not-found']
        #tsEnumNames: ['resourceNotFound']
      internalServerError:
        type: string
        enum: ['internal-server-error' ]
        #tsEnumNames: ['internalServerError' ]
