type: object
required:
  - errorCodes
additionalProperties: false
properties:
  errorCodes:
    type: object
    properties:
      resourceNotFound:
        type: string
        enum: ['resource-not-found']
      internalServerError:
        type: string
        enum: ['internal-server-error' ]
