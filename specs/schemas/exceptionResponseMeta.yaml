type: object
required:
  - code
additionalProperties: false
properties:
  code:
    type: string
    description: Meta information about related exception
  debug:
    type: object
    properties:
      debug:
        type: object
        description: related debug context object
      err:
        type: string
        description: related error string representation
      errObj:
        type: string
        description: Error object stringify
